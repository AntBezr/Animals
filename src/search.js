import React from 'react';

const search = () => {
  return (
    <div>
      
    </div>
  );
};

export default search;
/* import React from 'react';

const Search = (props) => {
  return (
    <input onChange={props.searchHandler}/>
  );
};

export default Search; */